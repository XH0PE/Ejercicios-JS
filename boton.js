// Crea un botón en HTML y utiliza JavaScript para detectar cuando se hace clic en él y mostrar un mensaje en la consola.
    const miBtn = document.getElementById('miBoton');

    miBtn.addEventListener('click', () => {
    console.log('mensaje')
})